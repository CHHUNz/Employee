spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 220320
    url: jdbc:postgresql://localhost:5432/employee_db
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
